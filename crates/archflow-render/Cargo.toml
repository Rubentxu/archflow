[package]
name = "archflow-render"
version.workspace = true
edition.workspace = true

[dependencies]
archflow-core = { path = "../archflow-core" }
archflow-engine = { path = "../archflow-engine" }
wgpu = { workspace = true }
bytemuck = { workspace = true }
pollster = "0.3"
