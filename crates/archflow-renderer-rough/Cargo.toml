[package]
name = "archflow-renderer-rough"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
archflow-core = { path = "../archflow-core" }
archflow-renderer = { path = "../archflow-renderer" }
rand = "0.8"

[lib]
path = "src/lib.rs"
