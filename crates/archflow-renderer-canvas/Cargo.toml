[package]
name = "archflow-renderer-canvas"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
archflow-core = { path = "../archflow-core" }
archflow-renderer = { path = "../archflow-renderer" }
wasm-bindgen = "0.2"
web-sys.workspace = true

[lib]
path = "src/lib.rs"
